# disposable-minecraft-multiplayer
A disposable multiplayer Minecraft server which retains only settings.


To run:

1) Navigate to folder of choice
2) Run server using command `docker run --net=host --name=mc-server -v ./data:/data danirali2007/disposable-minecraft-multiplayer`
